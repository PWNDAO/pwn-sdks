import type {
	Account,
	ERC20Balance,
	Loan,
	NFTAttribute,
	NFTBalance,
	NFTMetadata,
	Proposal,
} from "../generated/subgraph-QmXpWoxzBQ3dvDK3WTVAyMuEtDvm4UgqLWo7kFS4M6WXnh";

export function handleAccount(entity: Account): void {
	// Empty handler for Account
}

export function handleProposal(entity: Proposal): void {
	// Empty handler for Proposal
}

export function handleLoan(entity: Loan): void {
	// Empty handler for Loan
}

export function handleERC20Balance(entity: ERC20Balance): void {
	// Empty handler for ERC20Balance
}

export function handleNFTBalance(entity: NFTBalance): void {
	// Empty handler for NFTBalance
}

export function handleNFTMetadata(entity: NFTMetadata): void {
	// Empty handler for NFTMetadata
}

export function handleNFTAttribute(entity: NFTAttribute): void {
	// Empty handler for NFTAttribute
}
